<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form method="POST" action="/blob" enctype="multipart/form-data">
        <input type="file" name="user_file" id="blob" >
        <input type="submit" value="submit">
    </form>
    <script>
        fetch('https://picsum.photos/400/400')
        .then(res=>res.blob())
        .then(blob=>handler(blob));

        function handler(blob){
            console.log(blob);
            //problem with url is it keeps changing after every refresh
            const url=URL.createObjectURL(blob);
            console.log(url);
        }
        // let input=document.getElementById('blob');
        // export var url1;
        // convert files into temporary URL
        // input.addEventListener('change',function(){
        //     console.log(input.files[0]," is the input file");
        //     const url=URL.createObjectURL(input.files[0]);
        //     url1=url;
        //     console.log(url);
        // })

        
    </script>
</body>
</html>